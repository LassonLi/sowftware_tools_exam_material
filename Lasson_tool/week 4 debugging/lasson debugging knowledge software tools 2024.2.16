$:sudo apt install gdb

$:cc -Og -g stackcalc.c -o stackcalc

$:gdb ./stackcalc

(gdb) run <<< "1 2 +"

// breakpoint设位置要好一点，不然容易过不去
(gdb) b stackcalc.c:43

// breakpoint设位置要好一点，不然容易过不去
(gdb) b stackcalc.c:51

(gdb) run <<< "1 2 +"

// 目前已在第一个breakpoint，检查 is_num的值
(gdb) inspect is_num

// 跳去下一个breakboint
(gdb) continue 

// 目前已在第二个breakpoint，检查 is_num的值
(gdb) inspect is_num

// 清理所有breakpoint
(gdb) d